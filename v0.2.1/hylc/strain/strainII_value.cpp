#include "strain.hpp"
#ifdef hylc_strain_II

using namespace hylc;
using namespace hylc::mathematica;

Vec6 hylc::mathematica::strain(const Vec18 &xloc, const Mat2x2 &invDm,
                               const Vec3 &niexists) {
  // define output
  Vec6 out;

  Real copt1   = invDm(0, 0);
  Real copt2   = xloc(0);
  Real copt3   = -copt2;
  Real copt4   = xloc(3);
  Real copt5   = copt3 + copt4;
  Real copt6   = copt1 * copt5;
  Real copt7   = invDm(1, 0);
  Real copt8   = xloc(6);
  Real copt9   = copt3 + copt8;
  Real copt10  = copt7 * copt9;
  Real copt11  = copt10 + copt6;
  Real copt12  = Power(copt11, 2);
  Real copt13  = xloc(1);
  Real copt14  = -copt13;
  Real copt15  = xloc(4);
  Real copt16  = copt14 + copt15;
  Real copt17  = copt1 * copt16;
  Real copt18  = xloc(7);
  Real copt19  = copt14 + copt18;
  Real copt20  = copt19 * copt7;
  Real copt21  = copt17 + copt20;
  Real copt22  = Power(copt21, 2);
  Real copt23  = xloc(2);
  Real copt24  = -copt23;
  Real copt25  = xloc(5);
  Real copt26  = copt24 + copt25;
  Real copt27  = copt1 * copt26;
  Real copt28  = xloc(8);
  Real copt29  = copt24 + copt28;
  Real copt30  = copt29 * copt7;
  Real copt31  = copt27 + copt30;
  Real copt32  = Power(copt31, 2);
  Real copt33  = copt12 + copt22 + copt32;
  Real copt34  = Sqrt(copt33);
  Real copt35  = 1 / copt34;
  Real copt36  = invDm(0, 1);
  Real copt38  = invDm(1, 1);
  Real copt37  = copt36 * copt5;
  Real copt39  = copt38 * copt9;
  Real copt40  = copt37 + copt39;
  Real copt51  = Power(copt40, 2);
  Real copt42  = copt16 * copt36;
  Real copt43  = copt19 * copt38;
  Real copt44  = copt42 + copt43;
  Real copt52  = Power(copt44, 2);
  Real copt46  = copt26 * copt36;
  Real copt47  = copt29 * copt38;
  Real copt48  = copt46 + copt47;
  Real copt53  = Power(copt48, 2);
  Real copt54  = copt51 + copt52 + copt53;
  Real copt55  = Sqrt(copt54);
  Real copt56  = 1 / copt55;
  Real copt67  = xloc(9);
  Real copt72  = xloc(10);
  Real copt59  = -copt4;
  Real copt91  = xloc(11);
  Real copt74  = -copt15;
  Real copt84  = -copt25;
  Real copt116 = copt2 + copt59;
  Real copt117 = Power(copt116, 2);
  Real copt118 = copt13 + copt74;
  Real copt119 = Power(copt118, 2);
  Real copt120 = copt23 + copt84;
  Real copt121 = Power(copt120, 2);
  Real copt122 = copt117 + copt119 + copt121;
  Real copt123 = Sqrt(copt122);
  Real copt79  = -copt8;
  Real copt106 = -copt72;
  Real copt68  = -copt67;
  Real copt101 = -copt28;
  Real copt155 = Power(copt7, 2);
  Real copt157 = 1 / copt123;
  Real copt80  = copt4 + copt79;
  Real copt99  = copt18 + copt74;
  Real copt164 = xloc(15);
  Real copt62  = -copt18;
  Real copt169 = xloc(16);
  Real copt81  = copt23 * copt80;
  Real copt82  = copt25 * copt8;
  Real copt83  = -(copt28 * copt4);
  Real copt85  = copt28 + copt84;
  Real copt86  = copt2 * copt85;
  Real copt87  = copt81 + copt82 + copt83 + copt86;
  Real copt165 = -copt164;
  Real copt166 = copt165 + copt8;
  Real copt177 = xloc(17);
  Real copt63  = copt15 + copt62;
  Real copt179 = copt101 + copt177;
  Real copt197 = copt2 + copt79;
  Real copt198 = Power(copt197, 2);
  Real copt199 = copt13 + copt62;
  Real copt200 = Power(copt199, 2);
  Real copt201 = copt101 + copt23;
  Real copt202 = Power(copt201, 2);
  Real copt203 = copt198 + copt200 + copt202;
  Real copt204 = Sqrt(copt203);
  Real copt168 = copt164 * copt18;
  Real copt170 = -(copt169 * copt8);
  Real copt171 = copt169 + copt62;
  Real copt233 = Power(copt1, 2);
  Real copt235 = 1 / copt204;
  Real copt159 = copt13 * copt80;
  Real copt160 = copt15 * copt8;
  Real copt161 = -(copt18 * copt4);
  Real copt162 = copt2 * copt99;
  Real copt163 = copt159 + copt160 + copt161 + copt162;
  Real copt237 = xloc(12);
  Real copt241 = xloc(13);
  Real copt239 = copt237 + copt79;
  Real copt250 = xloc(14);
  Real copt183 = copt23 * copt63;
  Real copt184 = copt18 * copt25;
  Real copt185 = -(copt15 * copt28);
  Real copt186 = copt13 * copt85;
  Real copt187 = copt183 + copt184 + copt185 + copt186;
  Real copt251 = -copt250;
  Real copt252 = copt251 + copt28;
  Real copt265 = Power(copt80, 2);
  Real copt266 = Power(copt63, 2);
  Real copt102 = copt101 + copt25;
  Real copt267 = Power(copt102, 2);
  Real copt268 = copt265 + copt266 + copt267;
  Real copt269 = Sqrt(copt268);
  Real copt124 = -(copt18 * copt2 * copt25);
  Real copt125 = copt15 * copt2 * copt28;
  Real copt238 = -(copt18 * copt237);
  Real copt240 = copt15 * copt239;
  Real copt242 = -copt241;
  Real copt243 = copt18 + copt242;
  Real copt244 = copt243 * copt4;
  Real copt245 = copt241 * copt8;
  Real copt246 = copt238 + copt240 + copt244 + copt245;
  Real copt293 = copt1 + copt7;
  Real copt294 = Power(copt293, 2);
  Real copt296 = 1 / copt269;
  Real copt299 = Power(copt163, 2);
  Real copt300 = Power(copt87, 2);
  Real copt301 = Power(copt187, 2);
  Real copt302 = copt299 + copt300 + copt301;
  Real copt303 = Sqrt(copt302);
  Real copt247 = copt163 * copt246;
  Real copt248 = -(copt237 * copt28);
  Real copt249 = copt239 * copt25;
  Real copt253 = copt252 * copt4;
  Real copt254 = copt250 * copt8;
  Real copt255 = copt248 + copt249 + copt253 + copt254;
  Real copt256 = copt255 * copt87;
  Real copt257 = -(copt241 * copt28);
  Real copt258 = copt241 + copt62;
  Real copt259 = copt25 * copt258;
  Real copt260 = copt15 * copt252;
  Real copt261 = copt18 * copt250;
  Real copt262 = copt257 + copt259 + copt260 + copt261;
  Real copt263 = copt187 * copt262;
  Real copt264 = copt247 + copt256 + copt263;
  Real copt270 = copt18 * copt237 * copt25;
  Real copt271 = -(copt15 * copt237 * copt28);
  Real copt272 = copt2 * copt241 * copt25;
  Real copt273 = -(copt241 * copt25 * copt8);
  Real copt274 = -(copt2 * copt241 * copt28);
  Real copt275 = copt241 * copt28 * copt4;
  Real copt276 = copt23 * copt246;
  Real copt277 = -(copt15 * copt2 * copt250);
  Real copt278 = copt15 * copt250 * copt8;
  Real copt279 = copt18 * copt2 * copt250;
  Real copt280 = -(copt18 * copt250 * copt4);
  Real copt281 = -copt237;
  Real copt282 = copt281 + copt8;
  Real copt283 = copt25 * copt282;
  Real copt284 = copt237 * copt28;
  Real copt285 = -(copt250 * copt8);
  Real copt286 = copt101 + copt250;
  Real copt287 = copt286 * copt4;
  Real copt288 = copt283 + copt284 + copt285 + copt287;
  Real copt289 = copt13 * copt288;
  Real copt290 = copt124 + copt125 + copt270 + copt271 + copt272 + copt273 +
                 copt274 + copt275 + copt276 + copt277 + copt278 + copt279 +
                 copt280 + copt289;
  Real copt291 = copt269 * copt290;
  Real copt292 = ArcTan(copt264, copt291);
  Real copt295 = niexists(1);
  Real copt306 = Power(copt2, 2);
  Real copt307 = Power(copt13, 2);
  Real copt308 = Power(copt23, 2);
  Real copt309 = -2 * copt2 * copt4;
  Real copt310 = Power(copt4, 2);
  Real copt311 = -2 * copt13 * copt15;
  Real copt312 = Power(copt15, 2);
  Real copt313 = -2 * copt23 * copt25;
  Real copt314 = Power(copt25, 2);
  Real copt315 = copt306 + copt307 + copt308 + copt309 + copt310 + copt311 +
                 copt312 + copt313 + copt314;
  Real copt316 = Sqrt(copt315);
  Real copt317 = -2 * copt2 * copt8;
  Real copt318 = Power(copt8, 2);
  Real copt319 = -2 * copt13 * copt18;
  Real copt320 = Power(copt18, 2);
  Real copt321 = -2 * copt23 * copt28;
  Real copt322 = Power(copt28, 2);
  Real copt323 = copt306 + copt307 + copt308 + copt317 + copt318 + copt319 +
                 copt320 + copt321 + copt322;
  Real copt324 = Sqrt(copt323);
  Real copt326 = -2 * copt4 * copt8;
  Real copt327 = -2 * copt15 * copt18;
  Real copt328 = -2 * copt25 * copt28;
  Real copt329 = copt310 + copt312 + copt314 + copt318 + copt320 + copt322 +
                 copt326 + copt327 + copt328;
  Real copt330 = Sqrt(copt329);
  Real copt167 = copt13 * copt166;
  Real copt172 = copt171 * copt2;
  Real copt173 = copt167 + copt168 + copt170 + copt172;
  Real copt174 = copt163 * copt173;
  Real copt175 = copt166 * copt23;
  Real copt176 = copt164 * copt28;
  Real copt178 = -(copt177 * copt8);
  Real copt180 = copt179 * copt2;
  Real copt181 = copt175 + copt176 + copt178 + copt180;
  Real copt182 = copt181 * copt87;
  Real copt188 = -copt169;
  Real copt189 = copt18 + copt188;
  Real copt190 = copt189 * copt23;
  Real copt191 = copt169 * copt28;
  Real copt192 = -(copt177 * copt18);
  Real copt193 = copt13 * copt179;
  Real copt194 = copt190 + copt191 + copt192 + copt193;
  Real copt195 = copt187 * copt194;
  Real copt196 = copt174 + copt182 + copt195;
  Real copt205 = copt18 * copt2 * copt25;
  Real copt206 = -(copt15 * copt2 * copt28);
  Real copt207 = -(copt164 * copt18 * copt25);
  Real copt208 = copt15 * copt164 * copt28;
  Real copt209 = -(copt169 * copt2 * copt25);
  Real copt210 = copt169 * copt25 * copt8;
  Real copt211 = copt169 * copt2 * copt28;
  Real copt212 = -(copt169 * copt28 * copt4);
  Real copt213 = copt15 * copt166;
  Real copt214 = copt171 * copt4;
  Real copt215 = copt168 + copt170 + copt213 + copt214;
  Real copt216 = copt215 * copt23;
  Real copt217 = copt15 * copt177 * copt2;
  Real copt218 = -(copt15 * copt177 * copt8);
  Real copt219 = -(copt177 * copt18 * copt2);
  Real copt220 = copt177 * copt18 * copt4;
  Real copt221 = -(copt164 * copt28);
  Real copt222 = copt164 + copt79;
  Real copt223 = copt222 * copt25;
  Real copt224 = -copt177;
  Real copt225 = copt224 + copt28;
  Real copt226 = copt225 * copt4;
  Real copt227 = copt177 * copt8;
  Real copt228 = copt221 + copt223 + copt226 + copt227;
  Real copt229 = copt13 * copt228;
  Real copt230 = copt205 + copt206 + copt207 + copt208 + copt209 + copt210 +
                 copt211 + copt212 + copt216 + copt217 + copt218 + copt219 +
                 copt220 + copt229;
  Real copt231 = -(copt204 * copt230);
  Real copt232 = ArcTan(copt196, copt231);
  Real copt234 = niexists(2);
  Real copt58  = -(copt15 * copt8);
  Real copt60  = copt59 + copt8;
  Real copt61  = copt13 * copt60;
  Real copt64  = copt2 * copt63;
  Real copt65  = copt18 * copt4;
  Real copt66  = copt58 + copt61 + copt64 + copt65;
  Real copt69  = copt4 + copt68;
  Real copt70  = copt13 * copt69;
  Real copt71  = copt15 * copt67;
  Real copt73  = -(copt4 * copt72);
  Real copt75  = copt72 + copt74;
  Real copt76  = copt2 * copt75;
  Real copt77  = copt70 + copt71 + copt73 + copt76;
  Real copt78  = copt66 * copt77;
  Real copt88  = -(copt25 * copt67);
  Real copt89  = copt59 + copt67;
  Real copt90  = copt23 * copt89;
  Real copt92  = -copt91;
  Real copt93  = copt25 + copt92;
  Real copt94  = copt2 * copt93;
  Real copt95  = copt4 * copt91;
  Real copt96  = copt88 + copt90 + copt94 + copt95;
  Real copt97  = copt87 * copt96;
  Real copt98  = -(copt18 * copt25);
  Real copt100 = copt23 * copt99;
  Real copt103 = copt102 * copt13;
  Real copt104 = copt15 * copt28;
  Real copt105 = copt100 + copt103 + copt104 + copt98;
  Real copt107 = copt106 + copt15;
  Real copt108 = copt107 * copt23;
  Real copt109 = copt25 * copt72;
  Real copt110 = -(copt15 * copt91);
  Real copt111 = copt84 + copt91;
  Real copt112 = copt111 * copt13;
  Real copt113 = copt108 + copt109 + copt110 + copt112;
  Real copt114 = copt105 * copt113;
  Real copt115 = copt114 + copt78 + copt97;
  Real copt126 = copt18 * copt25 * copt67;
  Real copt127 = -(copt15 * copt28 * copt67);
  Real copt128 = copt2 * copt25 * copt72;
  Real copt129 = -(copt25 * copt72 * copt8);
  Real copt130 = -(copt2 * copt28 * copt72);
  Real copt131 = copt28 * copt4 * copt72;
  Real copt132 = -(copt18 * copt67);
  Real copt133 = copt67 + copt79;
  Real copt134 = copt133 * copt15;
  Real copt135 = copt106 + copt18;
  Real copt136 = copt135 * copt4;
  Real copt137 = copt72 * copt8;
  Real copt138 = copt132 + copt134 + copt136 + copt137;
  Real copt139 = copt138 * copt23;
  Real copt140 = -(copt15 * copt2 * copt91);
  Real copt141 = copt15 * copt8 * copt91;
  Real copt142 = copt18 * copt2 * copt91;
  Real copt143 = -(copt18 * copt4 * copt91);
  Real copt144 = copt68 + copt8;
  Real copt145 = copt144 * copt25;
  Real copt146 = copt28 * copt67;
  Real copt147 = -(copt8 * copt91);
  Real copt148 = copt101 + copt91;
  Real copt149 = copt148 * copt4;
  Real copt150 = copt145 + copt146 + copt147 + copt149;
  Real copt151 = copt13 * copt150;
  Real copt152 = copt124 + copt125 + copt126 + copt127 + copt128 + copt129 +
                 copt130 + copt131 + copt139 + copt140 + copt141 + copt142 +
                 copt143 + copt151;
  Real copt153 = copt123 * copt152;
  Real copt154 = ArcTan(copt115, copt153);
  Real copt156 = niexists(0);
  Real copt337 = Power(copt38, 2);
  Real copt339 = Power(copt36, 2);
  Real copt305 = copt36 + copt38;
  Real copt341 = Power(copt305, 2);
  out(0)       = copt34;
  out(1) =
      copt35 * (copt11 * copt40 + copt21 * copt44 + copt31 * copt48) * copt56;
  out(2) = copt55;
  out(3) = (-(copt154 * copt155 * copt156 * copt157) -
            copt232 * copt233 * copt234 * copt235 -
            copt292 * copt294 * copt295 * copt296) *
           copt303;
  out(4) = -(copt157 * copt235 * copt296 * copt303 *
             (copt292 * copt293 * copt295 * copt305 * copt316 * copt324 +
              copt330 * (copt1 * copt232 * copt234 * copt316 * copt36 +
                         copt154 * copt156 * copt324 * copt38 * copt7)));
  out(5) = copt303 * (-(copt154 * copt156 * copt157 * copt337) -
                      copt232 * copt234 * copt235 * copt339 -
                      copt292 * copt295 * copt296 * copt341);
  return out;
}

#endif  // hylc_strain_II
